import React from "react";

import styles from "../Styles/Components/MainButton.module.css";

const MainButton = () => {
  return (
    <div>
      <button className={styles.btnFinish}>Finish</button>
    </div>
  );
};

export default MainButton;
